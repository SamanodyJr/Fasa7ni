<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="50dp" />
    <solid android:color="@android:color/transparent" /> <!-- Background color, transparent in this case -->

    <stroke
        android:width="1dp"
        android:color="@color/support_grey" />

    <size
        android:width="71dp"
        android:height="66dp" />

</shape>