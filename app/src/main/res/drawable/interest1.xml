<!-- res/drawable/search_view_bg.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="600dp" /> <!-- Adjust the radius as needed -->
    <solid android:color="@color/extra_grey" /> <!-- Background color -->
    <stroke
        android:width="1dp"
        android:color="@color/main_grey" /> <!-- Stroke color and width (optional) -->
</shape>